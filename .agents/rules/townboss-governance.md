# TownBoss governance — always on

`D:\Projects\TownBoss` is the mandatory portfolio authority. Follow the repository `AGENTS.md`; it names the canonical governance corpus. Before substantial work, preflight the repository, worktree, branch, HEAD, upstream, status, and Phase/Deliverable/Task identifiers. Preserve unrelated work and safe-abort on unexpected state. Do not use destructive Git/recovery or broad formatter writes by default.

For UI work, apply the canonical TownBoss UX Engine reference audit and project UX authority before considering external patterns. References are subordinate; record lifecycle and implementation SHA, make fixture/real state explicit, collect rendered evidence, and require independent visual review. Tests and golden images alone are not visual acceptance.

Use the matching validation profile and keep automated, browser, physical, deployed, and operational evidence distinct. Before completion, reconcile project documentation and Memory first, then TownBoss; report exact evidence SHAs; create the compliant scoped checkpoint; and verify its remote SHA. Do not declare DONE while any closeout gate is pending.
